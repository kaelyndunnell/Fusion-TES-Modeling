FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    object      p;
}

dimensions      [0 2 -2 0 0 0 0];

internalField   uniform 0;

boundaryField
{
    tank_outlet
    {
        type            fixedValue;
        value           uniform 0; // need to define pressure at whichever inlet/outlet isn't defined in velocity 
    }
    tank_inlet
    {
        type            zeroGradient; 
    }
    tank_walls
    {
        type            zeroGradient;
    }
}
